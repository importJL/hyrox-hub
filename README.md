# Run and deploy the App

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Set the `OPENROUTER_KEY` in [.env](.env) to your Open Router API key
3. Run the app:
   `npm run dev`
